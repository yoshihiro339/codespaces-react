version: '3.8'
services:
  frontend:
    build: .
    ports:
      - "3000:80"
    depends_on:
      - backend
  backend:
    build: ./server
    ports:
      - "4000:4000"
    volumes:
      - ./server/prisma:/app/prisma
